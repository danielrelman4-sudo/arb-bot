[pytest]
pythonpath = .
testpaths = arb_bot/tests
# Exclude Phase 1-6 framework tests by default (not used by crypto engine).
# Run them explicitly with: python3 -m pytest arb_bot/tests/framework/
addopts = --ignore=arb_bot/tests/framework
